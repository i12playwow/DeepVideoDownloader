{
  "name": "deep-video-downloader",
  "version": "1.3.11",
  "description": "Desktop app: WebSocket server + MP4 download manager with advanced control and auto proxy boost.",
  "author": "DeepGrab",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --win && node scripts/check-dist-artifact.js",
    "check": "node check.js && node .freebuff/sync-demo.js --selftest && node scripts/check-release-drill.js",
    "test": "node test-config.js && node test-settings.js && node test-hls.js && node test-proxy.js && node test-range-server.js --selftest && node test-download-features.js && node test-e2e-harness.js",
    "boot-verify": "node scripts/boot-verify.js"
  },
  "dependencies": {
    "follow-redirects": "^1.15.6",
    "http-proxy-agent": "^7.0.2",
    "https-proxy-agent": "^7.0.5",
    "socks-proxy-agent": "^8.0.4",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "electron": "^43.4.1",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.deepgrab.downloader",
    "productName": "DeepVideoDownloader",
    "files": [
      "main.js",
      "preload.js",
      "browser-preload.js",
      "renderer.js",
      "browser.html",
      "renderer.html",
      "styles.css",
      "config.js",
      "downloader.js",
      "proxy.js",
      "lib",
      "extension"
    ],
    "asar": true,
    "asarUnpack": [
      "extension/**/*"
    ],
    "extraResources": [
      {
        "from": "vendor/ffmpeg/ffmpeg.exe",
        "to": "ffmpeg.exe"
      }
    ],
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
